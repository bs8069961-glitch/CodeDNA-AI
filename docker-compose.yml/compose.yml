version: "3.9"

services:

  api:

    build: .

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app

  neo4j:

    image: neo4j:5

    ports:
      - "7474:7474"
      - "7687:7687"

    environment:
      NEO4J_AUTH: neo4j/password